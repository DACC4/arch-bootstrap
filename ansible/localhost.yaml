---
- hosts: localhost
  connection: local
  become: true

  vars:
    aur_user: cc4

  roles:
    - base_packages
    - gnome
    - zsh
    - ssh_keygen
    - bluetooth
    - git
    - docker
    - snapshots
    - apps
    - browsers